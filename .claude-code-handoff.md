# Claude Code Handoff — HyperEdit / ClipWise

Timestamp: 2026-04-30T01:43:30Z

Repo: https://github.com/WaltLuv/hyperedit
Branch: main

## Setup already performed
- Dependencies installed with `npm install --legacy-peer-deps`.
- `.dev.vars` was created locally for runtime keys but is intentionally NOT committed.
- `package-lock.json` was updated and pushed.

## Latest pushed commit
- `8ea6c2466c6a1b7e471ba0d36745528ee92799c7`
- Message: `chore: update dependency lockfile`

## Important commands
```bash
npm install --legacy-peer-deps
npm run ffmpeg-server
npm run dev
npm run build
```

Local development requires two separate processes:
1. FFmpeg server on port 3333: `npm run ffmpeg-server`
2. Vite app on port 5173: `npm run dev`

## Environment variables needed locally
Create `.dev.vars` locally with:
```bash
GEMINI_API_KEY=...
FAL_API_KEY=...
GIPHY_API_KEY=...
OPENAI_API_KEY=...
```

Do not commit `.dev.vars` or secrets.

## Notes
- Project guidance is in `CLAUDE.md`.
- The local FFmpeg server is `scripts/local-ffmpeg-server.js`.
- Main React state is in `src/react-app/hooks/useProject.ts`.
- Main editor page is `src/react-app/pages/Home.tsx`.
